# Interne testscripts

Deze map bevat alleen interne testscripts voor ontwikkelaars.

## Regels
- Plaats geen `test*.php` bestanden meer in de project root.
- Voer testscripts alleen via CLI uit (`php scripts/tests/bestand.php`).
- Directe HTTP toegang tot testscripts is geblokkeerd via `.htaccess`.
